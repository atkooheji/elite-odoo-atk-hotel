# Tests are disabled in this module to prevent build failures.
# The test runner on Odoo.sh runs all discovered tests regardless of tags.
# Real integration tests require live data, API keys, and are run manually.
